#pragma once

#include "framework.h"

void createwindow_quote_list(WNDCLASSEX& wincl_2, HINSTANCE& hThisInstance, int nCmdShow, WCHAR szWindowClass[100]);
LRESULT CALLBACK windowprocessforwindow_quote_list(HWND handleforwindow2, UINT msg, WPARAM wParam, LPARAM lParam);
